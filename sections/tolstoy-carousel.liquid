<tolstoy-carousel data-tags="{% for tag in product.tags -%}{{ tag }},{%- endfor %}" class="tolstoy-carousel" id="{{ section.settings.publishId }}"  data-publish-id="{{ section.settings.publishId }}"  data-product-id="{{ product.id }}">
</tolstoy-carousel>

{% schema %}
  {
    "name": "Tolstoy carousel",
    "settings": [
      {
        "type": "text",
        "id": "publishId",
        "label": "Publish id",
        "default": "publishId"
      }
    ],
    "presets": [
{
"name": "Tolstoy carousel",
"category": "Tolstoy Sections"
}
]
  }
{% endschema %}